%h1 Rounds#new
= form_for @round do |f|
  = f.hidden_field :course_id
  .form_row
    = f.label :date
    = f.date_select :date
  .form_row
    = f.label :score
    = f.number_field :score, :min => 1
  .form_row
    = f.label :notes
    = f.text_area :notes

  .actions= f.submit
